// Define a global variable to hold the userInformation
let globalUserInformation;

// Function to initialize userInformation in the new JavaScript file
function initializeUserInformation(userInformation) {
  // Assign the userInformation to the global variable
  globalUserInformation = userInformation;

  // Use the userInformation as desired in this JavaScript file
  console.log(globalUserInformation);
}
